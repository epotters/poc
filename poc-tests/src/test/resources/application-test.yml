# General
spring:
  profiles:
    active: integration-tests
  banner:
    location: classpath:banner-test.txt

  # Data
#  datasource:
#    url: jdbc:mysql://localhost:23307/poc?useSSL=false&serverTimezone=Europe/Amsterdam
#    username: poc
#    password: b1vidh

# Logging
debug: true

logging:
  file: target/log/poc.log
  level:
    org.springframework.boot.autoconfigure.logging: INFO
    org.springframework.boot.core.io.support: INFO
    org.springframework.boot.test.annotation: INFO
    org.springframework.boot.web: INFO
    org.hibernate: WARN
    org.hibernateSQL: WARN


# Application to test
it:
  remote:
    host: "localhost"
    protocoll: "http"
    port: 8002
