{
  "name": "poc-dojo",
  "version": "1.0.0",
  "author": {
    "name": "Eelko Potters",
    "email": "epotters@xs4all.nl"
  },
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://epotters@bitbucket.org/epotters/poc.git"
  },
  "scripts": {
    "dev": "dojo build --mode dev --watch file --serve",
    "build": "dojo build --mode dist",
    "build:dev": "dojo build --mode dev",
    "test": "dojo test",
    "test:unit": "dojo build --mode unit && dojo test --unit --config local",
    "test:functional": "dojo build --mode functional && dojo test --functional --config local",
    "test:all": "dojo build --mode unit && dojo build --mode functional && dojo test --all --config local"
  },
  "dependencies": {
    "@dojo/framework": "^5.0.4",
    "@dojo/themes": "^5.0.1",
    "@dojo/widgets": "^5.0.0",
    "jwt-decode": "^2.2.0",
    "tslib": "~1.8.1"
  },
  "devDependencies": {
    "@dojo/cli": "^5.0.0",
    "@dojo/cli-build-app": "^5.0.4",
    "@dojo/cli-test-intern": "^5.0.0",
    "@types/node": "^9.6.48",
    "typescript": "^3.2.4"
  }
}
